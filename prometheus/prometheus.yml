# ================================
# Prometheus Configuration
# ================================

global:
  scrape_interval: 30s
  scrape_timeout: 10s
  evaluation_interval: 30s

# ================================
# Scrape Configurations
# ================================

scrape_configs:

  # --------------------------------------------------
  # SAMPLE JOB
  # --------------------------------------------------
  #
  # - job_name: "example-node-exporter"
  #
  #   # Override global interval if needed
  #   scrape_interval: 15s
  #   scrape_timeout: 10s
  #
  #   static_configs:
  #     - targets:
  #         - "node-exporter:9100"
  #
  # --------------------------------------------------
